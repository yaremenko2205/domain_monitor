# Cron endpoint protection
CRON_SECRET=your-random-secret-here

# Base URL for internal fetch calls (scheduler)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Default check schedule (overridden by DB settings)
CHECK_SCHEDULE=0 8 * * *
